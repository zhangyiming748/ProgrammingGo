package cow

import (
	"fmt"
	"strings"
)

const (
	plus  byte = 43
	sub   byte = 45
	mutil byte = 42
	left  byte = 40
	right byte = 41
)

func Expression_evaluation(s string) int {
	//plus := "+"
	//sub := "-"
	//mul := "*"
	b := []byte(s)
	for strings.Split(b, "") {
	}
}
